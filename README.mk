An mqtt bridge which takes a regular mqtt connection and pushes the data over a websocket connection to a target broker.

usage:
python3 app.py -s source broker IP address -p port -t target broker IP address -o port -e endpoint to publish as -t topic to bridge
